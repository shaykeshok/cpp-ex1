#include <iostream>
#include <stdexcept>
#include <vector>

using namespace std;

namespace phonetic{
    string find(string text,string word);
    vector<string> split(string text, bool caseSen);

};